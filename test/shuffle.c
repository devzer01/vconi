//
// Created by nayana on 11/23/17.
//

int main()
{
    int i;
    int SRC[8]; int DEST[8];
    for (i = 0; i < 7; i++) {
        if (SRC[(i * 8)+7] = 1 ) {
            DEST[(i * 8) + 7(i * 8) + 0] << 0;
        }
        else {
            index[2..0] ← SRC[(i*8)+2 .. (i*8)+0];
        DEST[(i*8)+7...(i*8)+0] ← DEST[(index*8+7)..(index*8+0)];
        endif;
    }
}